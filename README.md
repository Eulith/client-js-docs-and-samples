# Samples for Eulith Web3js client libraries

## Setup
`npm i`

## Build
`npm run build`

When you run the build command, the typescript output will end up in `./build`

## Run a specific example
`node build/<example_you_would_like_to_run>.js`

